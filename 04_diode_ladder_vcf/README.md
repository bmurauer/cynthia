#04: Diode Ladder VCF

## I/O Specification

Fill in the "Design" columns before building; fill in the "As-Built" columns after assembly and bench testing.

No `.net` file exists for this module yet, so the input/output rows below aren't populated from the schematic. Generate the netlist in KiCad (File > Fabrication Outputs > Netlist, or the equivalent export) and I can fill in the `Ref` / `Label` columns from it — or list the jacks here yourself.

### Inputs

| Ref | Label | Signal Type (Design) | Expected Behavior / Range (Design) | Measured Behavior / Range (As-Built) | Notes |
|-----|-------|-----------------------|--------------------------------------|-----------------------------------------|-------|
| | | | | | |

### Outputs

| Ref | Label | Signal Type (Design) | Expected Behavior / Range (Design) | Measured Behavior / Range (As-Built) | Notes |
|-----|-------|-----------------------|--------------------------------------|-----------------------------------------|-------|
| | | | | | |
